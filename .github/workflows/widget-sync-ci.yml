name: Widget Sync CI

on:
  push:
    branches: [ "main", "dev" ]
    paths:
      - "lib/widgets/**"
      - "lib/config/themes/**"
      - "lib/preview_v3/**"
      - "test/**"
      - "pubspec.yaml"
      - ".github/workflows/widget-sync-ci.yml"
  pull_request:
    branches: [ "main", "dev" ]
    paths:
      - "lib/widgets/**"
      - "lib/config/themes/**"
      - "lib/preview_v3/**"
      - "test/**"
      - "pubspec.yaml"
  workflow_dispatch:

jobs:
  widget-sync-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.32.4'

      - name: Install dependencies
        run: flutter pub get

      - name: Check Widget V3 preview registry is up to date
        run: dart run tool/generate_v3_preview_registry.dart --check

      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze project
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test

      - name: Build Widget V3 local web preview host
        run: flutter build web --release -t lib/preview_v3/main.dart
