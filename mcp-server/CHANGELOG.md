# Changelog

All notable changes to `flutter-widget-wallet-mcp` should be recorded in this file.

Format:
- Keep an `Unreleased` section at the top.
- Group entries under `Added`, `Changed`, `Fixed`, `Deprecated`, `Removed`, and `Security` when applicable.
- Move unreleased entries into a versioned section on release day.

## Unreleased

### Added

- Structured observability hooks for MCP tool calls and widget catalog operations.
- Remote-mode decision record and Phase 7 operational docs.

## 2.1.0 - 2026-07-04

### Added

- Installer support for Claude Code, Codex, and Cursor client configs.
- Local CI parity command, onboarding validation, evaluation runner, and Inspector verification flow.

### Changed

- MCP server split into reusable modules for dispatcher, installer, catalog parsing, and verification scripts.
