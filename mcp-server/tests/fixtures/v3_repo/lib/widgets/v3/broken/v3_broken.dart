import 'package:flutter/material.dart';
import '../../../config/themes/theme_color.dart';

class V3Broken extends StatelessWidget {
  const V3Broken({super.key});
  @override
  Widget build(BuildContext context) => Container(color: const Color(0xFF123456));
}
